<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">

        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/EtonBlue</item>
        <item name="android:textColor">@color/EagleGreen</item>
        <item name="colorPrimary">@color/IlluminatingEmerald</item>
        <item name="colorPrimaryDark">@color/EagleGreen</item>


        <item name="colorAccent">@color/PastelGray</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/background</item>
    </style>

</resources>
